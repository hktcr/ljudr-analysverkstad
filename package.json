{
  "name": "ljudr-analysverkstad",
  "version": "0.11.0",
  "private": true,
  "type": "module",
  "description": "Lokal analys och varsam trimning av fältinspelningar.",
  "scripts": {
    "build": "node scripts/build-site.mjs",
    "test": "node --test tests/*.test.mjs",
    "check": "node --check src/app.js && node --check src/wav.js && node --check src/dsp-core.js && node --check src/analysis-worker.js && node --check src/export-worker.js && node --check src/project.js && node --check sw.js",
    "serve": "python3 -m http.server 8080"
  },
  "engines": {
    "node": ">=20"
  },
  "license": "MIT"
}
